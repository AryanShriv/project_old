/**
 * G(Old) Design System - Border Radius
 * Subtle, restrained curves
 */

export const radius = {
  xs: 4,   // Tight radius for small elements
  sm: 8,   // Buttons, inputs
  md: 12,  // Cards, containers
  lg: 16,  // Large cards
  xl: 24,  // Hero elements
  full: 9999, // Circular (avatars, badges)
};